apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: signet-platform
resources:
  - namespace.yaml
  - postgres.yaml
  - bitcoind.yaml
  - signer.yaml
images:
  - name: signet-signer
    newName: signet-signer
    newTag: dev
