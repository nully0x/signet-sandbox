meta {
  name: environment.get
  type: http
  seq: 4
}

post {
  url: {{rpc_url}}
  body: json
  auth: none
}

headers {
  content-type: application/json
  Authorization: Bearer {{bearer}}
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 1,
    "method": "environment.get",
    "params": { "id": "{{env_id}}" }
  }
}
